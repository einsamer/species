'use strict';

describe('myApp.plantList module', function() {

  beforeEach(module('myApp.plantList'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('PlantListCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});